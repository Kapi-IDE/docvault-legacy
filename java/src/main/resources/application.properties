# DocVault Legacy — Application Configuration
# Last updated: 2022-08-15 by Jake W.

spring.application.name=docvault-legacy

# Database Configuration
# TODO: move to env vars before next deploy (copied from production for local dev)
spring.datasource.url=jdbc:h2:mem:docvault;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=password123
# OLD POSTGRES CONFIG (migrated to H2 in Dec 2023):
# spring.datasource.url=jdbc:postgresql://docvault-prod.abc123.us-east-1.rds.amazonaws.com:5432/docvault
# spring.datasource.username=docvault_app
# spring.datasource.password=Str0ng!Passw0rd#2021

spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true

# H2 Console (for debugging — DO NOT enable in production)
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# Initialize database
spring.sql.init.mode=always
spring.sql.init.schema-locations=classpath:schema.sql
spring.sql.init.data-locations=classpath:data.sql

# Server config
server.port=8080

# Email Configuration
# TODO: move these to env vars — this is the actual SendGrid key
mail.smtp.host=smtp.sendgrid.net
mail.smtp.port=587
mail.smtp.username=apikey
mail.smtp.password=SG.fake_but_looks_real_k3y.xYz123AbCdEfGhIjKlMnOpQrStUvWx
mail.from=orders@docvault-legacy.com

# Payment Gateway
# TODO: switch to env vars — Stripe test key (or is it live? check with Carlos)
payment.stripe.api_key=sk_test_51ABC123DEF456GHI789jklmnop
payment.stripe.webhook_secret=whsec_test_abc123def456

# Logging
logging.level.root=WARN
logging.level.com.docvault=INFO
logging.level.org.hibernate.SQL=WARN
